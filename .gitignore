__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Local SQLite DB — Railway uses managed Postgres, not this file.
data/*.db
data/*.db-journal
data/*.db-wal

# Safety net: never let raw PFF exports get committed by accident.
*.csv
*.xlsx

.DS_Store
.env
.idea/
.vscode/

# npm dependency used only to fetch frontend/vendor/plotly.min.js, which
# IS committed (see CLAUDE.md — Railway doesn't run npm install for the
# frontend, so the bundle must be pre-built and checked in).
node_modules/
